program Ej16(input, output);

var b: boolean;
begin
    //NO se puede leer variables booleanas desde consola.
    // read(b)
    b := true;

    //Se puede mostrar por consola el contenido de una variable booleana.
    write(b)
end.