//indica la direccion de memoria a la que apunta 
dispose(p) //p esta disponible para ser reasignada; p queda sin definir logicamente.

//avanza la variable de archivo a para que el siguiente componente de a quede disponible en a superior.
get(f)

//asignar dinamicamente memoria a una variable del tipo al que apunta p y asignar a p la direccion de esta direccion de memoria.
new(p)

//TODO: pagina 585 para completar procedimientos